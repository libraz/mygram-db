# MySQL integration tests
# Note: Most MySQL integration tests have been migrated to Python e2e tests.
# Remaining C++ integration tests that require MySQL are in tests/mysql/
# (binlog_reader_resource_test.cpp, sync_handler_test.cpp).
